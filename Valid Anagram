public class Solution {
    public bool IsAnagram(string s, string t) {
        char[] str1 = s.ToArray();
        char[] str2 = t.ToArray();
        Array.Sort(str1);
        Array.Sort(str2);
        return (str1.SequenceEqual(str2));
    }
}
